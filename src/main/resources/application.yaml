jwt:
  secret-key: 
server:
  error:
    include-messages: always
    include-binding-errors: always
spring:
  datasource:
    url: 
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
